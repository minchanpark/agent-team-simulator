# Project-local Codex hook config
# Codex appends a JSON payload argument when a turn completes.
notify = ["python3", ".codex/hooks/notify_dispatch.py"]

developer_instructions = """
This is a project-scoped instruction set for /Users/minchanpark/Documents/agent-team-simulator.
At the beginning of every new Codex session in this project, read these files before substantive work:
1) AGENTS.md
2) docs/PRD.md
3) docs/AGENT_GUIDELINES.md
4) docs/AGENT_PROMPTS.md
5) README.md

Treat these documents as the project source of truth for product intent, agent behavior, and implementation constraints.
If any required file is missing, continue with available files and state the missing file once.
You do not need to re-read these files on every turn unless they change.
"""
